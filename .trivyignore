# CVE-2026-32280: golang stdlib in docker-cli (Alpine package)
# Waiting for Alpine to ship docker-cli built with Go >= 1.25.9
# Tracked: only affects atlas-scheduler image
CVE-2026-32280

# CVE-2026-33671: picomatch ReDoS (>= 4.0.0, < 4.0.4)
# Our app uses picomatch 4.0.4 (lockfile). The 4.0.3 instance found by
# Trivy comes from globally-installed npm bundled dependencies, not our code.
# Mitigated by removing `npm install -g npm@latest` from Dockerfiles.
CVE-2026-33671
